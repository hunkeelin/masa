# Masa Configuration
# API keys can be set here or through the UI (UI setting takes precedence)

# OpenAI API Key (optional - can be set in UI)
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic Claude API Key (optional - can be set in UI)  
ANTHROPIC_API_KEY=your-claude-api-key-here

# Note: API keys are kept in memory only for security
# No sensitive data is stored permanently in files
CODING_KEYWORD_THRESHOLD=3
